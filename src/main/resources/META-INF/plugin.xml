<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.github.deadizar.aimanager</id>
    <name>AI Manager</name>
    <vendor>deadizar</vendor>
    <description><![CDATA[
        <p><b>AI Manager</b> is an IntelliJ Platform plugin scaffold for managing AI providers, sessions, and exports.</p>
        <p>This initial phase renames the template plugin, establishes the shared bundle, and prepares the codebase for the domain model and provider layers.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <resource-bundle>messages.MyBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow factoryClass="com.github.deadizar.aimanager.toolWindow.AiManagerToolWindowFactory" id="AI Manager"/>
        <postStartupActivity implementation="com.github.deadizar.aimanager.startup.AiManagerStartupActivity" />
        <applicationConfigurable parentId="tools" instance="com.github.deadizar.aimanager.settings.ui.AiManagerConfigurable" id="aimanager.settings" key="settings.displayName"/>
    </extensions>
</idea-plugin>
