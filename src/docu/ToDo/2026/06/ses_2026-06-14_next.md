# ToDo — Próxima sesión (desde 2026-06-14)

Orden recomendado de ejecución según dependencias técnicas: sub02 → sub01 → sub00, y sub03 → sub04, y sub05 al final.

---

## ALTO impacto

- [ ] **[agent] sub01.1 — Crear `AiProviderError` sealed class**
  Archivo nuevo: `provider/AiProviderError.kt`. 5 subtipos: Auth, Quota, Network, InvalidModel, Api.
  Mapear HTTP status codes en `OpenAiApiClient`. Referencia: `14-sub01-robustez.md` § Tarea 1.2.

- [ ] **[agent] sub01.2 — `RetryPolicy` con backoff exponencial**
  Archivo nuevo: `provider/RetryPolicy.kt`. Helper `withRetry`. Integrar en `OpenAiApiClient`.
  Añadir campos `maxRetries`/timeouts a `ProviderConfig` y `ProviderEntry`. Referencia: `14-sub01-robustez.md` § Tarea 1.1.

- [ ] **[agent] sub02.1 — Validación proactiva en `ProviderSettingsPanel`**
  Archivo nuevo: `settings/ProviderValidator.kt`. `DocumentListener` en URL/key/model.
  Labels rojo/verde inline. Referencia: `14-sub02-settings.md` § Tarea 2.2.

- [ ] **[agent] sub01.3 — Fallback automático de provider**
  Añadir `fallbackProviderInstanceIds` a settings. Loop de fallback en `AiManagerService.sendMessage`.
  UI de fallback chain en `ProviderSettingsPanel`. Referencia: `14-sub01-robustez.md` § Tarea 1.3.

- [ ] **[agent] sub00.1 — Streaming SSE en `ChatPanel`**
  Añadir `chatStream(): Flow<String>` a `AiProvider`. Implementar en `OpenAiApiClient` con `callbackFlow`.
  `sendMessageStreaming` en `AiManagerService`. ChatPanel con append progresivo.
  Referencia: `14-sub00-ux-chat.md` § Tarea 0.1.

---

## MEDIO impacto

- [ ] **[agent] sub02.2 — Botón "Test All" en `ProviderSettingsPanel`**
  Verificación en lote en pooled thread. Dialog de resultados con ✓/✗ por provider.
  Referencia: `14-sub02-settings.md` § Tarea 2.1.

- [ ] **[agent] sub02.3 — Botón "Diagnose" + `ProviderTestCache`**
  Archivo nuevo: `settings/ProviderTestCache.kt`. Dialog de diagnóstico sin revelar API key.
  Referencia: `14-sub02-settings.md` § Tarea 2.3.

- [ ] **[agent] sub00.2 — Rename / búsqueda / pin en `HistoryPanel`**
  Añadir `pinned: Boolean` a `Session`. SearchField + context menu + renderer con pin-first sort.
  Referencia: `14-sub00-ux-chat.md` § Tarea 0.2.

- [ ] **[agent] sub00.3 — Retry y Edit+Resend por mensaje**
  Añadir `id: String` a `Message`. `truncateAfterMessage` en `SessionManager`.
  Panel de `MessageRow`s con botones Retry/Edit en `ChatPanel`.
  Referencia: `14-sub00-ux-chat.md` § Tarea 0.3.

- [ ] **[agent] sub03.1 — Modelo `Artifact` y persistencia en `Session`**
  Archivo nuevo: `core/model/Artifact.kt`. Añadir `artifacts` a `Session`. `saveArtifact`/`openArtifact` en service.
  Referencia: `14-sub03-multimodal.md` § Tarea 3.1.

- [ ] **[agent] sub05.1 — E2E tests con MockWebServer**
  3 archivos nuevos en `src/test/.../e2e/`: `SessionFlowTest`, `ProviderFlowTest`, `ExportFlowTest`.
  Cubrir: flujo sesión completo, errores tipados, retry x3, fallback, todos los modos de export.
  Referencia: `14-sub05-calidad.md` § Tarea 5.1.

---

## BAJO impacto

- [ ] **[agent] sub04.1 — `ExportMetadata` en `LatexExporter`**
  Archivo nuevo: `core/export/ExportMetadata.kt`. Tabla de metadatos en .tex. Coste estimado hardcoded.
  Referencia: `14-sub04-export.md` § Tarea 4.1.

- [ ] **[agent] sub04.2 — Exporters Markdown y JSON**
  Archivos nuevos: `MarkdownExporter.kt`, `JsonExporter.kt`. Selector de formato en `ChatPanel`.
  Referencia: `14-sub04-export.md` § Tarea 4.2.

- [ ] **[agent] sub04.3 — Modos `CODE_ONLY` y `FULL_AUDIT`**
  Extraer `ExportMode` a archivo propio. Añadir 2 modos. Actualizar los 3 exporters.
  Referencia: `14-sub04-export.md` § Tarea 4.3.

- [ ] **[agent] sub03.2 — Preview embebido en `ImagePanel`, `TtsPanel`, `SttPanel`**
  `ImagePanel`: download + scale + JLabel. `TtsPanel`: AudioSystem playback. `SttPanel`: editable + copy.
  Export LaTeX con `\includegraphics`. Referencia: `14-sub03-multimodal.md` § Tarea 3.2.

- [ ] **[agent] sub03.3 — STT con selector de idioma**
  Añadir `language` param a `AiProvider.speechToText`. ComboBox en `SttPanel`. `cleanTranscript()`.
  Referencia: `14-sub03-multimodal.md` § Tarea 3.3.

- [ ] **[agent] sub05.2 — CI matrix en GitHub Actions**
  Archivo nuevo: `.github/workflows/ci.yml`. Matrix 2025.1 + 2025.2.6.2. `-PideVersion` en build.gradle.kts.
  Referencia: `14-sub05-calidad.md` § Tarea 5.2.

- [ ] **[agent] sub05.3 — Telemetría local opcional**
  Archivos nuevos: `core/telemetry/TelemetryEvent.kt`, `LocalTelemetry.kt`. Instrumentar service.
  Toggle en settings + "View metrics" dialog + "Export metrics JSON".
  Referencia: `14-sub05-calidad.md` § Tarea 5.3.
