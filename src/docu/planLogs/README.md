# planLogs

Immutable copies of agent-generated plans.

Path pattern:
`src/docu/planLogs/{branch}/{yyyy}-{mm}/{dd}-{title}.md`

Each plan should have an associated checkpoint progress file in the same branch/month scope.

